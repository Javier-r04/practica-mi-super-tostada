Cápsula de estado genérica, siempre en MAYÚSCULAS con tracking amplio.

```jsx
<Badge tone="green" dot>Sincronizado</Badge>
<Badge tone="amber">3 pendientes</Badge>
```

Para `estado` de pedido, factura o cola offline usa `EstadoBadge`, que fija el color por enum.
