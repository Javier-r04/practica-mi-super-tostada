Área de texto multilínea. Úsala para `notas_admin`, `nota_produccion` y el motivo obligatorio al reabrir un día.

```jsx
<Textarea label="Motivo de la reapertura" required rows={3} hint="Queda en audit_log con tu usuario." />
```
