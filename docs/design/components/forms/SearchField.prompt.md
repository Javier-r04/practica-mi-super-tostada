Buscador en cápsula con icono e X de limpiar.

```jsx
<SearchField value={q} onChange={setQ} placeholder="Cliente, alias o correlativo" />
```

En captura de pedido, busca por alias del cliente además del nombre canónico: el encargado escribe "tortilla grande", no "tortilla n.º 16".
