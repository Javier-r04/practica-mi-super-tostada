Selector nativo con rótulo.

```jsx
<Select label="Punto de carga" options={[{value:'PLANTA',label:'Planta'},{value:'DEMOCRACIA',label:'Democracia'}]} />
```

Sábado: la carga completa sale de planta, así que el selector se bloquea con `disabled` y una `hint` que lo explique.
